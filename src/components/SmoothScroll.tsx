"use client";
import { ReactNode } from "react";
// Native scrolling supports keyboard navigation, browser find, dialogs and reduced motion.
export default function SmoothScroll({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
